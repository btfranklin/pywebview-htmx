interface:
  display_name: "Use pywebview-htmx"
  short_description: "Build apps that consume pywebview-htmx"
  default_prompt: "Use $use-pywebview-htmx to implement or debug a declarative interaction in my application against its installed library version."
